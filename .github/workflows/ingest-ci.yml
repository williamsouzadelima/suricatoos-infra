name: ingest-ci

on:
  push:
    branches: [main, "feat/**"]
    paths: ["ingest/**", ".github/workflows/ingest-ci.yml"]
  pull_request:
    paths: ["ingest/**", ".github/workflows/ingest-ci.yml"]

permissions:
  contents: read

jobs:
  build-test:
    name: build & test (ingest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ingest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: ingest/go.mod
      - name: gofmt
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then echo "$unformatted"; exit 1; fi
      - name: go vet
        run: go vet ./...
      - name: build
        run: go build ./...
      - name: test (race)
        run: go test -race ./...
