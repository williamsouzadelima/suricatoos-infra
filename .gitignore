src/
*.log

__pycache__/
*.pyc
.venv/
.pytest_cache/
dist/

# Compiled Go binaries (built via Dockerfile for release, never committed)
/sensor/sensor-agent
/agent/suricatoos-agent
*.test
# Snippets locais de nginx (hostnames de infra ficam fora do repo).
compose/nginx/extra/*.conf
# .env do deploy (endereços de bind, caminhos do host).
compose/.env
# Redes de administração (faixas internas ficam fora do repo).
compose/nginx/admin-nets/*.conf
